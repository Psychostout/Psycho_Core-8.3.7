===========================================================================
 Copyright (c) 2026 Psychostout. All rights reserved.
 Original work authored 100% from scratch for Psycho_Core.
 Licensed under LICENSE.MYCODE (see LICENSE.MYCODE.txt in the repo root).
 NOT covered by the base GPL framework license. Development/evaluation only.
===========================================================================

================================================================================
 PSYCHO_CORE 8.3.7 - CHANGELOG
================================================================================
 Purpose : Record every change made to the codebase / project.
 Format  : [DATE] (category) description  -- by agent/user
 Note    : Categories = [SETUP] [CORE] [SQL] [BUILD] [CONFIG] [SCRIPT]
                        [FIX] [DOC] [DEP]
================================================================================


--------------------------------------------------------------------------------
[2026-06-07] BASELINE
--------------------------------------------------------------------------------
[SETUP]  Cloned repository Psycho_Core-8.3.7 (TrinityCore-based, BfA 8.3.x).
           - Branch: main | HEAD: a02d2b5 ("Base")
           - 15,439 files, ~2.1 GB. Integrity verified (git fsck clean).
[DOC]    Created Dev/ folder for project tracking.
[DOC]    Added Dev/Chatlog.txt    - running conversation log.
[DOC]    Added Dev/Changelog.txt  - this file.
[DOC]    Added Dev/Psycho_CoreREADME.txt - build/plan/reference doc.

--------------------------------------------------------------------------------
[2026-06-07] STEP 2 - VERSION BUMPS (CMake) + DEP TARGETING
--------------------------------------------------------------------------------
[BUILD]  CMakeLists.txt
           - cmake_minimum_required(VERSION 3.8) -> (VERSION 4.3.2)
           - Updated accompanying comment.
[BUILD]  contrib/protoc-bnet/CMakeLists.txt
           - cmake_minimum_required(VERSION 3.8) -> (VERSION 4.3.2)
[DOC]    Recorded recommended safe target dependency versions (see README):
           - Boost  : 1.83 (target), avoid >=1.87
           - OpenSSL: 3.0.x LTS (requires code patches below first)
           - MariaDB: 10.6.3 (sole recommended DB)
[DOC]    Updated Chatlog.txt + Psycho_CoreREADME.txt for Step 2.

--------------------------------------------------------------------------------
 Files modified this step: 2  (CMakeLists.txt, contrib/protoc-bnet/CMakeLists.txt)
 No C++ source modified yet.
--------------------------------------------------------------------------------


================================================================================
 PENDING / PLANNED CHANGES  (identified during Step 2 audit - need approval)
================================================================================
 P-01 [BUILD] dep/cotire/CMake/cotire.cmake: cmake_minimum_required(2.8.12)
        CMake 4.x rejects minimums < 3.5. Must bump (e.g. to 3.5) or replace
        cotire. BLOCKS configure under CMake 4.3.2.

 P-02 [BUILD] cmake/macros/FindOpenSSL.cmake: OPENSSL_MAX_VERSION "1.2" cap
        rejects OpenSSL 3.x. Must raise cap (e.g. to "3.5") and adjust
        OPENSSL_EXPECTED_VERSION to "1.1.1". Required for OpenSSL 3 bump.

 P-03 [CORE] src/common/Cryptography/OpenSSLCrypto.cpp
        Legacy threading callbacks removed in OpenSSL 1.1+. Guard with
        #if OPENSSL_VERSION_NUMBER < 0x10100000L (make threadsSetup/Cleanup
        no-ops on 1.1+/3.x). Required for OpenSSL 3 bump.

 P-04 [CORE] src/common/Cryptography/RSA.cpp (~line 101)
        Direct opaque-struct access (_rsa->n) illegal on 1.1+/3.x.
        Replace with RSA_get0_key()/BN_copy of returned BIGNUM.
        Required for OpenSSL 3 bump.

 (P-02..P-04 to be executed together when we proceed with OpenSSL 3.x.)


--------------------------------------------------------------------------------
[2026-06-07] STEP 3 - README CONVERSION (Legion 7.3.5 -> Psycho_Core 8.3.7)
--------------------------------------------------------------------------------
[DOC]    README.md  (overwrote the stock TrinityCore README)
           - Converted user's LegionCore 7.3.5 README to Psycho_Core 8.3.7.
           - Re-targeted: Legion 26972 / EXPANSION_LEGION -> BfA 34769 /
             EXPANSION_BATTLE_FOR_AZEROTH (verified in SharedDefines.h).
           - Corrected C++ standard claim C++20 -> C++14 (actual).
           - Badges now: CMake 4.3.2, C++14, Boost 1.83, OpenSSL 3.0 LTS,
             MariaDB 10.6.3, BfA 8.3.0 (34769).
           - REMOVED content that does not exist / does not apply in this repo:
               * SoloCraft section + Solocraft.Enable config
               * CMakePresets.json + preset-based quick start
               * compile_deps/ (setup_deps scripts, vendored MariaDB Connector,
                 DEPENDENCIES.md, LegionCoreDeps.cmake)
               * docs/ guides (BUILD_WINDOWS, DEPENDENCY_SETUP, PORTABLE_SERVER...)
               * Reforged merge table (BattlePay, BrawlersGuild, Draenor,
                 Demon Invasion, etc.)
               * "bnetserver builds" green status badge (NOT built/verified yet)
               * French->English config narrative; unverifiable Windows toolchain
                 claims (MSVC 19.30 / _WIN32_WINNT bump) - reset to this repo's
                 real MSVC 19.24 floor.
           - Build-status section now honest: "no full build verified yet" and
             lists blockers P-01..P-04 from Step 2.
           - Repo layout / requirements / config knobs rewritten to match the
             real files (worldserver.conf.dist 4191 lines/~599 settings;
             bnetserver.conf.dist 403 lines; src/tools extractors present).
[DOC]    Updated Chatlog.txt + Psycho_CoreREADME.txt for Step 3.

--------------------------------------------------------------------------------
 Files modified this step: 1  (README.md). No C++/build source modified.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 3b - HIDE DEV TRACKING FROM PUBLIC README
--------------------------------------------------------------------------------
[DOC]    README.md
           - Removed the Dev/ subtree (Chatlog.txt / Changelog.txt /
             Psycho_CoreREADME.txt) from the "Repository layout" block.
           - Removed the "Dev tracking" row from the Highlights table.
           - Result: README.md contains NO reference to the Dev/ folder.
           - Dev/ folder retained on disk (private to user + agent).
[DOC]    Updated Chatlog.txt + Psycho_CoreREADME.txt.

--------------------------------------------------------------------------------
 Files modified this step: 1  (README.md).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 4 - VERIFICATION PASS (no code changes)
--------------------------------------------------------------------------------
[DOC]    Verified all Step 2/3 report claims against actual file contents.
           - CMake 4.3.2: present in CMakeLists.txt + contrib/protoc-bnet. OK.
           - Boost 1.83: Asio guards present (IoContext.h), code unmodified. OK.
           - P-01 cotire 2.8.12, P-02 FindOpenSSL 1.0/1.2 cap, P-03
             OpenSSLCrypto legacy callbacks, P-04 RSA.cpp _rsa->n : all still
             present and UNPATCHED, exactly as reported. OK.
           - MariaDB 10.6.3: doc-only; no build-system pin added. OK.
[NOTE]   ENVIRONMENT: repo .git directory no longer present in workspace
           (excluded from snapshots / did not persist). Working-tree files are
           intact. Change tracking will rely on this Dev/ log, not git diff.

 CONFIRMED CHANGE INVENTORY (cumulative, Steps 1-3b):
   MODIFIED: CMakeLists.txt, contrib/protoc-bnet/CMakeLists.txt, README.md
   ADDED   : Dev/Chatlog.txt, Dev/Changelog.txt, Dev/Psycho_CoreREADME.txt
   PENDING : P-01 cotire.cmake, P-02 FindOpenSSL.cmake,
             P-03 OpenSSLCrypto.cpp, P-04 RSA.cpp  (not yet applied)

--------------------------------------------------------------------------------
 Files modified this step: 0 source/build (verification only) + Dev docs.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 5 - APPLIED OPENSSL-3 / CMAKE-4 BLOCKER PATCHES + DB RESEARCH
--------------------------------------------------------------------------------
[BUILD]  dep/cotire/CMake/cotire.cmake  (P-01)
           - cmake_minimum_required(VERSION 2.8.12) -> (VERSION 3.5)
           - Reason: CMake 4.x removed compat for minimums < 3.5.
[BUILD]  cmake/macros/FindOpenSSL.cmake  (P-02)
           - OPENSSL_EXPECTED_VERSION "1.0" -> "1.1.1"
           - OPENSSL_MAX_VERSION      "1.2" -> "3.5"
           - Now accepts OpenSSL 3.x (target 3.0 LTS) instead of rejecting it.
[CORE]   src/common/Cryptography/OpenSSLCrypto.cpp  (P-03)
           - Guarded legacy thread-locking callbacks behind
             #if OPENSSL_VERSION_NUMBER < 0x10100000L.
           - Added no-op threadsSetup()/threadsCleanup() for OpenSSL >=1.1.0/3.x
             (libssl is internally thread-safe there; call sites unchanged).
           - Verified branch selection with the C preprocessor (cpp).
[NOTE]   P-04 src/common/Cryptography/RSA.cpp - NO CHANGE NEEDED.
           Re-audit: the `_rsa->n` access is already inside the pre-1.1.0
           #else branch; the >=1.1.0 path already uses RSA_get0_key().
           Step 2 had over-flagged this; corrected here.
[DOC]    Identified correct WORLD database for this core:
           - TDB 837.20101 (2020-10-20), TrinityCore master/BfA 8.3.x branch.
           - World file : TDB_full_world_837.20101_2020_10_20.sql
           - Hotfixes   : TDB_full_hotfixes_837.20101_2020_10_20.sql
           - Archive    : TDB_full_837.20101_2020_10_20.7z (GitHub release).
           - sql/base/ here ships only auth + characters; world/hotfixes come
             from this TDB. Updates.EnableDatabases = 15 (all 4 DBs).

 BLOCKER STATUS AFTER THIS STEP:
   P-01 DONE | P-02 DONE | P-03 DONE | P-04 N/A (already guarded)
   -> The 4 reported blockers are now resolved for an OpenSSL 3.x + CMake 4.3.2
      configure/compile. (Full build still not yet executed in this env.)

--------------------------------------------------------------------------------
 Files modified this step: 3 build/source
   (cotire.cmake, FindOpenSSL.cmake, OpenSSLCrypto.cpp) + Dev docs.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 6 - VERIFY P-01..P-04 + MODULE RESEARCH
--------------------------------------------------------------------------------
[BUILD]  cmake/macros/FindOpenSSL.cmake  (P-02 HARDENING)
           - During deep verification found ENSURE_VERSION_RANGE uses an
             EXCLUSIVE upper bound, so MAX "3.5" would reject OpenSSL 3.5.x.
           - Raised OPENSSL_MAX_VERSION "3.5" -> "3.6" (added comment).
           - Now accepts full 1.1.1 .. 3.5.x; rejects 1.0.x. Re-tested via
             Python replication of the cmake normalize/compare math.
[VERIFY] Confirmed P-01 (cotire 3.5), P-03 (OpenSSLCrypto guards balanced +
           header/namespace/include order correct), P-04 (RSA.cpp only opaque
           access is in the pre-1.1.0 #else; 3.x uses RSA_get0_key) all correct.
[VERIFY] Bonus: HMAC.h + CryptoHash.h legacy ctx APIs already version-guarded;
           ARC4/AES use EVP_CIPHER_CTX (RC4 = 3.x legacy provider, runtime note).
[DOC]    Added Dev/ModuleSupport_Research.txt - module-support integration plan
           (3 options; recommends Option B: AC-style top-level modules/ folder
           feeding the core's existing script system). NO module code written.
[DOC]    Updated Chatlog.txt + Psycho_CoreREADME.txt.

 BLOCKER STATUS: P-01 DONE | P-02 DONE (hardened) | P-03 DONE | P-04 N/A.

--------------------------------------------------------------------------------
 Files modified this step: 1 build/source (FindOpenSSL.cmake cap 3.5->3.6)
   + 1 new doc (ModuleSupport_Research.txt) + Dev docs.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 7 - OPTION B DEEP RESEARCH (NO CODE CHANGES)
--------------------------------------------------------------------------------
[DOC]    Added Dev/ModuleSupport_Research_Deep.txt - file-by-file integration
           plan for the AC-style modules/ system, validated against THIS repo:
             * ~10/11 helper macros already exist; only CopyModuleConfig +
               ConfigureModules.cmake + modules/CMakeLists.txt + modules/ dir new.
             * Build flow + worldserver linkage + hot-reload path traced.
             * Module SQL auto-applies via the updates_include DB table.
             * Loader '-' naming gotcha noted; BfA vs WotLK source caveat noted.
             * Integration steps S1-S7 + open decisions D1-D6 listed.
[NOTE]   NO core/build files modified this step (research only, per request).

--------------------------------------------------------------------------------
 Files modified this step: 0 build/source.
   New doc: Dev/ModuleSupport_Research_Deep.txt + Dev docs updated.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 9 - IMPLEMENT MODULE SUPPORT (Option B) + SKELETON + DOCS
--------------------------------------------------------------------------------
[BUILD]  NEW cmake/macros/ConfigureModules.cmake (module-base helper macros).
[BUILD]  NEW modules/CMakeLists.txt (module build engine; static/dynamic).
[BUILD]  NEW modules/ModulesLoader.cpp.in.cmake + modules/ModulesLoader.h.
[BUILD]  EDIT CMakeLists.txt: include(ConfigureModules); add_subdirectory(modules)
           BEFORE src (guarded by MODULES != none).
[BUILD]  EDIT cmake/options.cmake: add MODULES option (default "static"),
           per-module MODULE_<N> cache vars, force shared libs if a module is
           dynamic (IsDynamicLinkingModulesRequired).
[BUILD]  EDIT src/server/worldserver/CMakeLists.txt: link 'modules' + depend on
           WORLDSERVER_DYNAMIC_MODULES_DEPENDENCIES.
[CORE]   EDIT src/server/game/Scripting/ScriptMgr.cpp: forward-declare and call
           AddModulesScripts() after the static script loader in Initialize().
[FIX]    Skeleton bug: GetLevel() -> getLevel() (this 8.3 tree uses lowercase).
[MODULE] NEW modules/mod-skeleton/ (working example: PlayerScript + WorldScript,
           loader, conf, example world SQL, optional .cmake, README).
[DOC]    NEW docs/HOW_TO_BUILD_A_MODULE.md, docs/HOW_TO_INSTALL_MODULES.md,
           modules/README.md, Dev/ModuleAPI_Reference.txt.
[DOC]    README.md: added a "Modules" section.

 MODULE SYSTEM: static default (matches core SCRIPTS default), dynamic allowed,
 per-module overrides via MODULE_MOD_<NAME>; hot reload available with dynamic.
 NOTE: not compile-tested in this sandbox (no cmake/boost/openssl/mariadb here);
 verified by header/signature cross-check + CMake structure balance.

--------------------------------------------------------------------------------
 Files modified this step: 4 edits (CMakeLists.txt, options.cmake,
   worldserver/CMakeLists.txt, ScriptMgr.cpp) + many new module/doc files.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 8 - README LOGO + FINAL MODULE-PLAN VERIFICATION
--------------------------------------------------------------------------------
[ASSET]  Added assets/Psychocore.swordofsarg2.png (copied from user upload).
[DOC]    README.md - inserted the PsychoCore logo in TWO places:
           - centered under the title (# Psycho_Core 8.3.7 ...)
           - centered in the middle of the page (before "## Repository layout")
           - used <p align="center"><img src="assets/..." width="420"></p>.
[VERIFY] Final module-support check (NO code written):
           - Confirmed SCRIPTS default = "static" (options.cmake L25) -> setting
             MODULES default = "static" is correct/consistent. (static recommended,
             dynamic allowed.)
           - Confirmed per-module cache var pattern + the
             WITH_DYNAMIC_LINKING_FORCED mechanism we must mirror for modules.
           - Confirmed wire-up points (top CMakeLists includes, src/server
             add_subdirectory order, worldserver link + dynamic-dep hook).
           - VERDICT: Option B plan is 100% confirmed; awaiting user go/no-go.

--------------------------------------------------------------------------------
 Files modified this step: 1 doc (README.md) + 1 new asset. NO build/source.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 10 - VERIFY + LICENSE.MYCODE HEADERS ON FROM-SCRATCH FILES
--------------------------------------------------------------------------------
[VERIFY] Full code verification - no issues. APIs/templates/CMake all confirmed.
[FIX]    Removed dead code (unused CONFIG_FILE_NAME) in CopyModuleConfig.
[LEGAL]  Copied LICENSE.MYCODE.txt to repo root.
[LEGAL]  Added short MyCode copyright header (-> LICENSE.MYCODE.txt) to the 20
           from-scratch files ONLY (module system + skeleton + docs + Dev txt).
           Loader-template + modules/CMakeLists carry a clause-5 derivative note.
[LEGAL]  Confirmed the 9 EDITED TrinityCore files carry NO MyCode header.

 Files this step: header added to 20 new files + LICENSE.MYCODE.txt added;
   1 dead-code cleanup (ConfigureModules.cmake). NO edited-TC file got a header.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 10b - REMOVE MYCODE HEADER FROM BUILD-GLUE/BOILERPLATE
--------------------------------------------------------------------------------
[LEGAL]  User decided CMake/build-glue files don't count as "his" code.
           Removed the LICENSE.MYCODE header from:
             cmake/macros/ConfigureModules.cmake
             modules/CMakeLists.txt
             modules/ModulesLoader.cpp.in.cmake   (derivative TC loader template)
             modules/mod-skeleton/mod-skeleton.cmake
             modules/ModulesLoader.h              (boilerplate plumbing decl)
[LEGAL]  RULE NOW: MyCode header only on genuine original content -
           module C++ logic (mod_skeleton.cpp/_loader.cpp), conf, sql, and our
           docs/Dev txt. All .cmake/CMakeLists/.in templates + the loader
           plumbing header = NO header (framework glue / derivative boilerplate).
[LEGAL]  Final headered set = 15 files (see Chatlog Step 10b).

--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 11 - RESEARCH: mod-playerbots port feasibility (no code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/Research_mod-playerbots_Port.txt (feasibility study).
           Finding: NOT a drop-in; needs core-fork edits + full rewrite for BfA
           8.3 (talents/spells/opcodes/bot-session). ~9.5/10 difficulty, months.
           Options O1-O4 + recommendation documented.
[NOTE]   No core/module code changed this step (research only).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 11.2 - RESEARCH: all bot options compared (no code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/Research_Bot_Options_Compared.txt comparing ike3, mangosbot,
           cmangosbot, mod-playerbots, Psychostout/Trinity-Bots (NPCBots) and
           Psychostout/Mangos-Bots (your ike3 playerbot).
           Verdict: NPCBots (Trinity-Bots) is the least-bad port (~7.5/10, same
           TC core family, creature-based); all real-Player playerbots ~9-9.5/10
           (core-family + BfA expansion gaps). All GPL-2.0 (port stays GPL).
[NOTE]   No core/module code changed this step (research only).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 11.3 - mod-playerbots code review (no code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/Research_modplayerbots_CodeReview.txt.
           Verdict: NOT mechanically portable - ServerFacade is not a real
           abstraction; core API calls inlined everywhere; AGPLv3.
           Plan: DOCUMENT exhaustively -> CLEAN-ROOM REWRITE for 8.3, reusing
           the Strategy/Action/Value/Trigger architecture as a blueprint.
           Scale: ~600-900 files, ~200-300k LOC (PlayerbotAI.cpp alone 218KB).
[NOTE]   No core/module code changed this step (research/review only).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 11.4 - ike3/mangosbot vs mod-playerbots as study source (no code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/Research_ike3_vs_modplayerbots.txt.
           Verdict: study/rewrite from ike3/mangosbot (celguar/mangosbot-bots),
           not mod-playerbots. Reason: mangosbot keeps a real ServerFacade
           (~32 methods) + multi-expansion design = clean blueprint; mod-
           playerbots stripped the abstraction (inlined -> "scatter"). Same AI
           engine in both. mangosbot is GPL-2.0 (aligns w/ our base) and is what
           the user already runs. Plan updated: mangosbot PRIMARY, mod-playerbots
           SECONDARY reference.
[NOTE]   No core/module code changed this step (research only).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] REVIEW - Psychostout/Mangos-Bots playerbot (chosen source) (no code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/Research_PsychostoutMangosBots_Review.txt.
           Confirmed as best recreate source: lean canonical ike3 engine
           (PlayerbotAI 55KB), already Cataclysm 4.3.4, GPL-2.0, user-known +
           extended. Real DK impl (Blood/Frost/Unholy) + Cata-Patch-V1
           (SmartScale/BotActiveAlone/etc). No ServerFacade in this branch ->
           we add an 8.3 facade. Plan: document -> facade -> Phase A core edits
           -> vertical slice (Death Knight first) -> scale.
[NOTE]   No core/module code changed this step (review only).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] RESEARCH - newest/most feature-packed bot source (no code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/Research_Newest_BotSource.txt.
           Winner: cmangos/playerbots (official CMaNGOS, forked from celguar) -
           newest (June 2026 commits), most feature-packed (301 actions/197
           values, ServerFacade, TravelMgr, ahbot, BG/open-world pop), GPL-2.0,
           but WotLK-only. Plan: use it as PRIMARY engine blueprint +
           ServerFacade contract; YOUR Mangos-Bots as Cata/feature blueprint
           (DK + Cata-Patch); mod-playerbots secondary naming ref only.
[NOTE]   No core/module code changed this step (research only).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 12 - cloned + deep-dived cmangos/playerbots (no core code)
--------------------------------------------------------------------------------
[RESEARCH] Cloned cmangos/playerbots -> /home/user/playerbots (OUTSIDE our repo;
           172MB, 1028 files, ~238k LOC). Verified intact; repo not polluted.
[DOC]    Added Dev/playerbots_docs/00_MASTER_STUDY.txt - full study:
           AI engine (Strategy/Action/Value/Trigger/Context), the ServerFacade
           #ifdef core-switch seam (add TRINITY/BfA branch), deep-dives on
           spells+ranks (BfA removed ranks; DB2 SpellInfo), talents (WotLK
           points/tabs -> BfA spec grid), pathing (CMaNGOS MotionMaster/Travel
           -> TC MotionMaster/PathGenerator), core hooks (Phase A), DB, code
           style, and the 7-stage "same result" port plan.
[NOTE]   No Psycho_Core core/module code changed (study only). cmangos source
           kept separate from our repo.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 12.1 - deeper study + cross-ref + 4-stage plan (no core code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/playerbots_docs/01_PORT_PLAN_4STAGE.txt.
           Cross-referenced cmangos engine vs OUR core (spell cast/ranks/talents/
           movement/session) - verified. Mined user's Mangos-Bots (DK pattern,
           Cata-Patch features, CharacterHandler) for reuse. 4 stages: (1)
           foundation+main AI w/ following+fighting bot, (2) classes+talents (DK
           vertical slice first), (3) world behaviour + user's scaling features,
           (4) group content+extras. Includes also-need list, 8 verification
           checks, and 4 open questions (packaging/Monk+DH/v1 scope/client data).
[NOTE]   No Psycho_Core code changed (plan only). cmangos source stays separate.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 12.3 - refined plan: mod-psychobot + Monk/DH (no core code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/playerbots_docs/02_PLAN_REFINED_mod-psychobot.txt.
           - Build as MODULE "mod-psychobot" (rename skeleton) + minimal Phase A
             core edits; -DPSYCHOBOT seam.
           - Monk(10)+DemonHunter(12) CONFIRMED in our core (classes/powers/specs
             via sChrSpecializationStore); cmangos lacks them -> author via
             copy-rename (warrior base), verify spells/specs in DB2. Researched
             both classes' specs/spells.
           - V1 scope DECIDED = alts-as-bots (V2 = world population + Cata-Patch
             scaling).
           - Open blocker: 8.3.7 client data (mmaps/vmaps/DB2). Mixed-license tree.
           - 4 stages re-affirmed + 10 verification checks.
[NOTE]   No Psycho_Core code changed (plan only). cmangos source stays separate.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 12.4 - BUILT mod-psychobot STAGE 1 (Foundation + Main AI)
--------------------------------------------------------------------------------
[MODULE] Renamed modules/mod-skeleton -> modules/mod-psychobot (skeleton retired).
[MODULE] NEW modules/mod-psychobot (13 files, ~1066 LOC), all original/clean-room
         (MyCode header; AI architecture inspired by ike3/cmangos GPL-2.0 but
         written fresh against TrinityCore 8.3 APIs):
           src/ai/PsychobotEngine.{h,cpp}     - Strategy/Action/Trigger engine
           src/ai/PsychobotAI.{h,cpp}         - per-bot brain + TC-8.3 ServerFacade
                                                seam (cast/target/movement)
           src/ai/PsychobotStrategies.{h,cpp} - follow-master + basic-combat
           src/PsychobotMgr.{h,cpp}           - global bot registry, ticked
           src/mod_psychobot.cpp              - WorldScript(tick)+PlayerScript
                                                (logout)+CommandScript(.psychobot)
           src/mod_psychobot_loader.cpp       - Addmod_psychobotScripts()
           conf/mod_psychobot.conf.dist, mod-psychobot.cmake, README.md
[VERIFY] Cross-checked EVERY core API used vs headers (Player/Unit/MotionMaster/
         SpellMgr/DB2/ObjectAccessor) - all present. Fixed includes (WorldSession.h,
         MotionMaster.h, DBCEnums.h, cmath/algorithm); fixed LocalizedString
         access (Name[LOCALE_enUS]); fixed IsMoving() precedence. Brace/paren
         balance OK on all files. GetVictim ambiguity resolved (Unit:: line 994).
[VERIFY] TICK PATH CONFIRMED end-to-end: World::Update -> sScriptMgr->OnWorldUpdate
         -> FOREACH_SCRIPT(WorldScript)->OnUpdate -> psychobot_WorldScript ->
         sPsychobotMgr->UpdateAI. NO core edits needed for Stage 1 (V1 manages
         already-online chars; socketless auto-login Phase A deferred to later).
[NOTE]   Not compile-tested (sandbox lacks cmake/boost/openssl/mariadb); verified
         by exhaustive header cross-reference + structural checks. Real build is
         the final proof (needs the prepared toolchain + 8.3.7 client data).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 12.5 - mod-psychobot STAGE 2: CLASSES + BfA TALENTS
--------------------------------------------------------------------------------
[RESEARCH] BfA 8.3 talents = 7 tiers x 3 columns (unlock L15/30/45/60/75/90/100),
           rested-area only; PvP talents separate. Full 12-class/36-spec/role
           matrix researched (wowpedia/icy-veins/warcrafttavern).
[BUILD]  NEW ai/PsychobotTalentMgr.{h,cpp} - GENERIC BfA talent application for
           ALL classes: EnsureSpecialization (SetPrimarySpecialization +
           ActivateTalentGroup via sDB2Manager) + ApplyTalents (per unlocked
           tier, LearnTalent first valid column). Uses the exact core pattern
           (GetTalentsByPosition(getClass(),t,c)) - bounds-safe.
[BUILD]  NEW ai/PsychobotClassAI.{h,cpp} - data-driven spell-PRIORITY rotation;
           resolves spell NAMES -> known spell IDs at runtime via the ServerFacade
           seam (no hardcoded WotLK IDs; unknown spells skipped, never crash).
           Resolves current spec via sChrSpecializationStore OrderIndex.
[BUILD]  NEW ai/PsychobotClassRegistry.cpp - rotation tables for ALL 12 classes
           / 36 specs (role-aware). DEATH KNIGHT = full vertical slice
           (Blood/Frost/Unholy populated). Monk(10) + Demon Hunter(12) added as
           net-new. Others seeded with researched signature spells (refine once
           8.3.7 client DB2 loaded).
[BUILD]  WIRED ClassAI into PsychobotAI: EnsureSpecAndTalents() on first tick;
           combat strategy AttackTargetAction now runs DoClassRotation (+ keeps
           autoattack/MoveChase). Added .psychobot spec <name> <index> command +
           PsychobotMgr::SetSpec.
[VERIFY] 15 files, ~1557 LOC, all brace/paren balanced. Every core API cross-
           checked vs headers (talent/spec/spell/DB2). DBCEnums.h include added.
           GetTalentsByPosition bounds confirmed safe (matches core usage).
           Spec ordering correct (spec set before talents).
[NOTE]   HONEST: rotations seeded with researched signature spell NAMES; exact
           correctness/tuning is confirmed only after 8.3.7 client DB2 is loaded
           (name->id resolution). The FRAMEWORK is complete; per-spec spell lists
           are refinement. Not compile-tested (no toolchain in sandbox).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 12.6 - mod-psychobot STAGE 3: WORLD BEHAVIOUR + SCALING
--------------------------------------------------------------------------------
[RESEARCH] Mined user's Mangos-Bots Cata-Patch for the exact scaling design +
           config keys (BotActiveAlone, SmartScale, DisabledWithoutRealPlayer,
           gear quality/persistence, sync-level). Verified core APIs (TeleportTo,
           GiveLevel, StoreNewItemInBestSlots/EquipNewItem, CanUseItem,
           GetItemTemplate, World::GetAllSessions, SelectNearbyTarget, GameTime).
[BUILD]  NEW PsychobotPopulationMgr.{h,cpp} - random-bot population + the full
           scaling system replicated from the user's Cata-Patch:
             * BotActiveAlone (% active when alone) + rotating active roster.
             * SmartScale (linear active% reduction as world tick ms rises).
             * DisabledWithoutRealPlayer (idle when no real player online).
             * ShouldBotBeActive() gate + force-active near a real player.
             * HasRealPlayerOnline/IsRealPlayerNear via World sessions.
           Config-gated (off by default). Open-world SPAWN top-up marked TODO
           (needs Phase A socketless login); scaling/roster logic complete now.
[BUILD]  NEW ai/PsychobotGearMgr.{h,cpp} - level-appropriate gear logic:
           IsAcceptableItem (quality cap + level + CanUseItem proficiency),
           GearUp (persistence-gated), EnsureBasics. Exact per-slot item pick
           needs client item DB2 -> safe no-op until then (no hardcoded IDs).
[BUILD]  Strategies: added NoMasterTrigger + GrindAction (masterless bots seek
           nearby hostiles to level via SelectNearbyTarget) + RestAction (stop &
           regen when hurt/safe); wired into the follow strategy.
[BUILD]  Wired scaling into PsychobotAI::UpdateAI (ShouldBotBeActive gate +
           GearUp); population ticked from PsychobotMgr::UpdateAI; config
           (re)loaded in WorldScript::OnConfigLoad. Added ~18 conf keys.
[VERIFY] 19 files, ~2033 LOC, all brace/paren balanced. Every core API cross-
           checked vs headers (sWorld/sessions, teleport, level, item/CanUseItem,
           SelectNearbyTarget, GameTime, EQUIP_ERR_OK). Includes consistent.
[NOTE]   HONEST: open-world SPAWNING of new bots needs Phase A (socketless
           login) - the spawn top-up is a marked TODO. The scaling decisions,
           gear logic, and world-behaviour all operate correctly on managed bots
           now. Exact gear/consumable item picks need the 8.3.7 item DB2.
           Not compile-tested (no toolchain in sandbox).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 12.7 - mod-psychobot STAGE 4: GROUP CONTENT + EXTRAS
--------------------------------------------------------------------------------
[BUILD]  NEW PsychobotGroupMgr.{h,cpp} - grouping + roles:
           InviteToGroup (create/join master's party via Group::Create/AddMember
           + sGroupMgr->AddGroup), AssignRole (SetLfgRoles from spec role),
           GetBotRole (spec->SpecRole via ClassRegistry), GetGroupAssistTarget
           (assist tank/leader target), GetGroupHealTarget (most-injured member),
           InBattleground/InDungeon.
[BUILD]  NEW PsychobotAhBot.{h,cpp} - auction-house bot scaffold (config-gated,
           safe no-op until item DB2 available).
[BUILD]  COMBAT now group-aware: PsychobotAI::GetCurrentTarget prefers the group
           assist target; ClassAI::DoRotation routes HEALERS to heal the most-
           injured friendly (GetGroupHealTarget) instead of the enemy.
[BUILD]  Commands: .psychobot group <name> (PsychobotMgr::GroupBot). AhBot ticked
           + config-loaded from the WorldScript. Added ahbot conf keys.
[VERIFY] 23 files, ~2409 LOC, all brace/paren balanced. Every Stage-4 core API
           cross-checked vs headers (Group Create/AddMember/RemoveMember/IsFull/
           GetLeaderGUID/GetMemberSlots/SetLfgRoles/isLFGGroup, sGroupMgr,
           ObjectAccessor::GetPlayer, lfg::PLAYER_ROLE_*, InBattleground).
           Group() ctor public; LFG.h path correct; includes complete.
[NOTE]   HONEST: BG/Arena/dungeon TACTICS (map-specific positioning/objectives)
           are framework-level (role assist/heal works; per-map objective AI is a
           data task). AhBot posting needs item DB2. Phase A socketless login
           still pending for offline-alt/world spawning. Not compile-tested.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] STEP 12.8 - FULL RE-AUDIT of mod-psychobot (pre-compile)
--------------------------------------------------------------------------------
[VERIFY] 29-point skeptical re-audit of all 4 stages (23 source files, 2409 LOC,
         13 .cpp). FINDINGS - all PASS:
   1  Header methods vs definitions: all declared methods defined (Mgr/AI/Engine).
   2  Destructors defined where complete types visible (unique_ptr<ClassAI>/Engine
      ok via PsychobotAI.cpp includes). No incomplete-type errors.
   3  Build collection: CollectSourceFiles recurses src/ + src/ai/ -> all 13 .cpp.
   4  SpecRole enum fwd-decl matches definition (enum class : uint8) across files.
   5  Singletons (sWorld/sGroupMgr/sObjectMgr/sConfigMgr/sDB2Manager/sSpellMgr/
      sChrSpecializationStore/sSpellNameStore) all have their headers included.
   6  std headers (<algorithm>/<cstdlib>/<set>/<memory>/<vector>/<string>/<cmath>)
      all present where used.
   7  Header guards all unique. 8 Aggregate inits match struct fields.
   9  CanUseItem(ItemTemplate const*) overload exists; gear logic resolves.
   10 DB2Manager calls correctly qualified (sDB2Manager.X). 
   11 Default arg (Event()) only in header, not redeclared in cpp.
   12 BUILD CHAIN end-to-end: modules cfg before src -> worldserver links
      'modules' -> ScriptMgr::Initialize calls AddModulesScripts ->
      Addmod_psychobotScripts -> AddSC_mod_psychobot -> 3 scripts. OK.
   13 LICENSE: all 23 source files carry MyCode header (all original). OK.
   14 All conf switches default OFF (safe). 15 Distinct loader vs mod-skeleton.
   16 Earlier blockers still fixed (cotire 3.5, OpenSSL cap 3.6, CMake 4.3.2).
   17 Only 2 TODOs = safe no-ops (ahbot posting, Phase A pop top-up). No crashers.
   18 Brace/paren balance: all 23 files balanced.
[NOTE]   Caveat unchanged: NOT compile-tested (no toolchain/client data here).
         Audit is exhaustive static cross-reference. Phase A (socketless login)
         is the only intentionally-deferred feature.
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] FAT PORT - study + plan (Path A); NO code changed
--------------------------------------------------------------------------------
[DOC]    Added Dev/Mod-Psychobots-Ref.txt - full reference architecture study
           (engine tick flow, primitives, registry, per-class pattern, shared
           libs, managers, expansion #ifdef, dependency surface ~3137 names, and
           a "what I might have missed" list).
[DOC]    Added Dev/playerbots_docs/05_FAT_PORT_PLAN.txt - the fat-port plan:
           KEEP packaging+managers; REPLACE thin engine; build fat engine ->
           shared libs -> 12 class folders. Milestones M1..M14.
[DECISION] Core-engine replacement (not full restart). Awaiting GO for M1.
[NOTE]   No code changed this step (report-first, as requested).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] FAT PORT - master per-file checklist (no engine code yet)
--------------------------------------------------------------------------------
[DOC]    Added Dev/Mod-Psychobots-Checklist.txt - ALL 985 reference files (18
           sections), each annotated with its classes + a [ ] tick + "=>" slot.
           Marked [x] the 8 already covered by our managers/talents (977 remain).
           Includes progress summary, milestone->section map, "missed?" list,
           and NEW Monk + Demon Hunter (no-reference) author entries.
[DECISION] Confirmed: core-engine replacement (keep packaging+managers, replace
           thin engine, build fat engine + shared libs + class folders).
[NOTE]   No engine code changed this step (checklist + plan; report-first).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] FAT PORT - validated + split into 29 ordered steps (no code)
--------------------------------------------------------------------------------
[DOC]    Added Dev/playerbots_docs/06_FAT_PORT_STEPS.txt: 29 ordered steps in 4
           phases (engine core / shared libs+rewire / 12 classes / depth+
           integration incl. Phase-A socketless login). Validated build order via
           engine #include graph + keystone (CastSpellAction family) + actual
           class dependency surface. Synced checklist STEP->section map.
[NOTE]   No code changed (validation + planning; report-first).
--------------------------------------------------------------------------------


--------------------------------------------------------------------------------
[2026-06-07] FAT PORT S1 - engine primitives
--------------------------------------------------------------------------------
[BUILD]  NEW modules/mod-psychobot/src/engine/: AiObject.{h,cpp} (+ ActionPriority
           / ActionThreatType), Event.h, ActionBasket.h, NamedObjectContext.h
           (Qualified/Factory/Context/List). NEW src/PsychobotAIFwd.h bridge +
           PsychobotAIBridge impl in PsychobotAI.cpp (avoids engine<->AI cycle).
[VERIFY] Brace/paren balanced; unique guards; include dirs resolve (engine/ auto-
           added); bridge parity; MyCode headers. Not compile-tested (no toolchain).
[DOC]    Ticked 7 reference files on the checklist (15 done / 970 todo).
--------------------------------------------------------------------------------

================================================================================
 2026-06-08  PHASE B - S2..S6  (engine core finished + shared libs + REWIRE)
================================================================================
[S2]     Engine value/action graph primitives (all MyCode-headered):
           engine/Value.h (UntypedValue, Value<T>, Calculated/Single/Memory),
           engine/NextAction.{h,cpp} (Size/Clone/Merge/Array/Destroy),
           engine/Action.{h,cpp} (Execute/IsPossible/IsUseful, P/A/C virtuals),
           engine/ActionNode.h (P/A/C merge + ACTION_NODE_P/A/C macros).
[S3]     Engine decision core:
           engine/Trigger.h (Trigger + TriggerNode), engine/Multiplier.h,
           engine/Strategy.{h,cpp} (+ BotState, StrategyType, ActionNode factory),
           engine/Queue.{h,cpp} (relevance pop, dedupe, expiry),
           engine/AiObjectContext.h (4 chained NamedObjectContextLists +
           templated GetValue<T>), engine/Engine.{h,cpp} (DoNextAction:
           ProcessTriggers -> PushDefaultActions -> pop highest -> multipliers
           -> prereq/alt/continuer walk, depth-bounded), engine/ServerFacade.
           {h,cpp} (TC 8.3 core-API seam: liveness/range/aura/power/cooldown).
[S4]     Core VALUES -> values/CoreValues.{h,cpp} (11: target/self/master target,
           health, power-pct, distance, in-combat, has-aura, target-has-aura,
           spell-ready, party-member-to-heal) + RegisterCoreValues.
         Core TRIGGERS -> triggers/CoreTriggers.{h,cpp} (14: low/medium/critical
           health, has/no target, enemy in/out of melee, target casting, has/no
           aura, target-has-no-aura, spell-ready, party-member-low-health) +
           RegisterCoreTriggers.
[S5]     KEYSTONE CastSpellAction family -> actions/GenericSpellActions.{h,cpp}
           (Cast / CastMelee / CastAura / CastBuff / CastMeleeAoe /
           CastRangedDebuff / CastPet; qualifier = spell NAME, range-gated,
           auto-prereq "reach melee"/"reach spell"). Movement+attack ->
           actions/MovementActions.{h,cpp} (reach melee, reach spell, follow
           master, stop, attack).
[S6]     Generic STRATEGIES -> strategies/GenericStrategies.{h,cpp} (follow,
           melee, ranged, tank, heal). AiFactory -> ai/PsychobotAiFactory.{h,cpp}
           (CreateContext registers all base contexts; per-state engine wiring by
           spec role). Spec->role table -> ai/PsychobotSpecRoles.{h,cpp} (12
           classes / 36 specs). PsychobotAI REWRITTEN to own one AiObjectContext
           + 3 per-state Engines (NonCombat/Combat/Dead) and the cast/move bridge
           seam; PsychobotGroupMgr re-pointed to SpecRoles.
[DEL]    Thin engine removed (replaced by real engine): ai/PsychobotEngine.{h,cpp},
           ai/PsychobotClassAI.{h,cpp}, ai/PsychobotClassRegistry.cpp,
           ai/PsychobotStrategies.{h,cpp}.
[VERIFY] All 35 new/changed files: braces/parens balanced; guards unique; relative
           ../engine,../values,... includes resolve; bridge declared==defined
           (10/10); no references to deleted headers; Strategy::GetName override
           parity OK; Qualified MI (dynamic_cast) sound; core APIs cross-checked
           (MoveChase/Attack/SetSelection/GetSelectedUnit/HasCooldown/GetPowerPct/
           IsWithinMeleeRange/isInFront/CastSpell(Unit*,id,bool)). NOT compile-
           tested (no toolchain / no 8.3.7 client data) - first real compile on
           desktop is the truth check.
[DOC]    Checklist progress 15 -> 53 done / 932 todo; S2-S6 created-equivalents
           block appended. >>> MILESTONE: bot follows + chases + auto-attacks +
           (once class strategies land in S7+) casts via the REAL engine. <<<

================================================================================
 2026-06-08  PHASE C - S7  DEATH KNIGHT (first full class vertical slice)
================================================================================
[S7]     BfA 8.3 Death Knight, all 3 specs, driven by the real engine:
           classes/deathknight/DKTriggers.{h,cpp} - DK conditions reading core
             values by NAME (DB2-agnostic): runic power high/enough (power::6),
             frost/blood/virulent plague absent (target has aura by name),
             killing machine / rime / sudden doom procs (has aura by name),
             bone shield low. + RegisterDeathKnightTriggers.
           classes/deathknight/DKStrategies.{h,cpp} - GenericDKStrategy (Death
             Strike emergency heal, Death Grip pull) + Blood (Marrowrend/Bone
             Shield, Heart Strike, Death Strike RP dump, Blood Boil, Death and
             Decay) + Frost (Howling Blast/Frost Fever + Rime, Obliterate +
             Killing Machine, Frost Strike RP, Pillar of Frost CD) + Unholy
             (Outbreak/Virulent Plague, Festering/Scourge Strike, Death Coil +
             Sudden Doom, Apocalypse CD, Death and Decay) + GenericDKNonCombat
             (Raise Dead ghoul, Horn of Winter). All casts via the keystone
             CastSpell family, referenced by spell NAME.
           classes/deathknight/DKAiObjectContext.{h,cpp} - bundles DK triggers+
             strategies; CombatStrategyForSpec(0=blood/1=frost/2=unholy).
[ENGINE] Added keystone CastSelfSpellAction ("cast self") - self-cast summon/CD
           with no target/aura gate (Raise Dead, Horn of Winter, Army, etc.).
         Core values: aura / spell-ready qualifiers now accept a spell NAME
           (resolved via the bot) as well as a numeric id -> class wiring is
           DB2-agnostic (ResolveQualifierSpell in CoreValues.cpp).
[WIRE]   AiFactory: CreateContext layers DK context for DK bots; InitCombatEngine
           installs "dk" + spec strategy for DK (generic melee/ranged/tank/heal
           for other classes); InitNonCombatEngine adds "dk nc". SpecRoles gained
           GetBotSpecIndex(). EnsureSpecAndTalents already rebuilds the combat
           engine after spec applies, so a DK gets blood/frost/unholy correctly.
[VERIFY] 6 new DK files + edits: braces/parens balanced; guards unique; all
           includes resolve; every trigger/strategy override defined; GetValue<T>
           2-arg overload exists; Queue dedups same-named nodes; POWER_RUNIC_POWER
           =6 confirmed; CLASS_DEATH_KNIGHT path confirmed; rotation traced end-to-
           end (proc reactions > disease upkeep > RP dump > rune builders). NOT
           compile-tested (no toolchain/8.3.7 DB2). Spell NAMES from BfA 8.3 DK
           guides; exact id resolution happens at runtime against client DB2.
[DOC]    Checklist: DK section (20 ref files) marked; progress 53 -> 73 done.
           >>> MILESTONE: DK bot plays a real 8.3 rotation end-to-end. <<<

================================================================================
 2026-06-08  PHASE C - S8  WARRIOR (Arms / Fury / Protection + stances)
================================================================================
[S8]     BfA 8.3 Warrior, all 3 specs, on the real engine:
           classes/warrior/WarriorTriggers.{h,cpp} - rage high/enough (power::1),
             target low health (Execute window <=20%), sudden death proc,
             overpower ready, enrage absent, battle/defensive stance absent,
             shield block absent. + RegisterWarriorTriggers.
           classes/warrior/WarriorStrategies.{h,cpp}:
             Generic (Charge gap-closer, Shield Wall/Die by the Sword defensive),
             Arms (Mortal Strike key, Overpower proc, Execute, Slam rage dump,
               Colossus Smash CD, Whirlwind AoE),
             Fury (Rampage @ high rage / Enrage refresh, Bloodthirst, Raging Blow,
               Execute, Recklessness CD, Whirlwind cleave),
             Protection (Shield Slam on CD, Revenge, Devastate, Shield Block +
               Ignore Pain mitigation, Thunder Clap AoE),
             NonCombat (Battle/Defensive stance upkeep).
           classes/warrior/WarriorAiObjectContext.{h,cpp} - bundle + spec map
             (0=arms,1=fury,2=protection).
[WIRE]   AiFactory: CLASS_WARRIOR registers Warrior context, installs "warrior"
           + spec strategy in combat, "warrior nc" in non-combat.
[VERIFY] 6 new files + factory edits: braces/parens balanced (whole module);
           guards unique; all includes resolve; every override defined; POWER_RAGE
           =1 + warrior spec order (0/1/2) confirmed; rotation traced (Arms:
           Execute window beats fillers). Per-bot class-scoped contexts mean
           "protection"/"fury" names don't collide across classes. NOT compile-
           tested (no toolchain/8.3.7 DB2); spell NAMES from BfA 8.3 guides.
[DOC]    Checklist: Warrior section (16 ref files) marked; progress 73 -> 89.

================================================================================
 2026-06-08  PHASE C - S9  PALADIN (Holy / Protection / Retribution + auras)
================================================================================
[S9]     BfA 8.3 Paladin, all 3 specs (FIRST HEALER), on the real engine:
           classes/paladin/PaladinTriggers.{h,cpp} - holy power high (power::9,
             >=60% ~ 3 HP), target low health (Hammer of Wrath <=20%), devotion
             aura absent, shield of righteous absent. + RegisterPaladinTriggers.
           classes/paladin/PaladinStrategies.{h,cpp}:
             Generic (Hammer of Wrath execute, Lay on Hands/Divine Shield save),
             Holy (Holy Shock + Word of Glory + Flash of Light on the party
               member to heal via cast heal; Judgment/Crusader Strike filler),
             Protection (Avenger's Shield, Judgment, Hammer of the Righteous,
               Consecration AoE, Shield of the Righteous mitigation),
             Retribution (Crusader Strike/Judgment/Blade of Justice HP gen,
               Templar's Verdict 3 HP, Divine Storm AoE, Avenging Wrath CD),
             NonCombat (Devotion Aura + Blessing of Kings upkeep).
           classes/paladin/PaladinAiObjectContext.{h,cpp} - bundle + spec map
             (0=holy,1=protection,2=retribution).
[ENGINE] Added keystone CastHealSpellAction ("cast heal") - casts a friendly
           heal on the "party member to heal" value (most-injured group member,
           or self solo); 40yd, no chase. Reused by all future healer specs.
[WIRE]   AiFactory: CLASS_PALADIN registers Paladin context, installs "paladin"
           + spec strategy in combat, "paladin nc" in non-combat. Healer heals
           use ACTION_*_HEAL relevance tiers so a hurt ally outranks any DPS.
[VERIFY] 6 new files + keystone/factory edits: whole module braces/parens
           balanced; guards unique; all includes resolve; every override defined;
           bridge decl==def; POWER_HOLY_POWER=9 + paladin spec order (0/1/2)
           confirmed; heal rotation traced (hurt member -> Holy Shock@70 beats
           DPS filler@11). NOT compile-tested (no toolchain/8.3.7 DB2); spell
           NAMES from BfA 8.3 guides.
[DOC]    Checklist: Paladin section (10 ref files) marked; progress 89 -> 99.
           Added Dev/remember_prompt.txt (chat-loss recovery brief).

================================================================================
 2026-06-08  PHASE C - S10  HUNTER (Beast Mastery / Marksmanship / Survival + pets)
================================================================================
[S10]    BfA 8.3 Hunter, all 3 specs, on the real engine (research: wowhead.com):
           classes/hunter/HunterTriggers.{h,cpp} - focus high/enough (power::2),
             target low health (Kill Shot <=20%), no pet, pet hurt (<60%),
             serpent sting absent. + RegisterHunterTriggers.
           classes/hunter/HunterStrategies.{h,cpp}:
             Generic (Kill Shot execute, Exhilaration self-heal),
             Beast Mastery (ranged/pet: Kill Command/Barbed Shot/Cobra Shot,
               Bestial Wrath CD, Multi-Shot AoE; revives/summons pet first),
             Marksmanship (ranged: Aimed Shot/Arcane Shot/Steady Shot, Serpent
               Sting DoT, Rapid Fire/Trueshot CDs, Multi-Shot AoE),
             Survival (MELEE/pet: Kill Command/Raptor Strike/Wildfire Bomb,
               Serpent Sting, Coordinated Assault CD, Carve AoE; pet upkeep),
             NonCombat (Call Pet 1 / Revive Pet / Mend Pet upkeep).
           classes/hunter/HunterAiObjectContext.{h,cpp} - bundle + spec map
             (0=beast mastery,1=marksmanship,2=survival).
[ENGINE] ServerFacade gained pet helpers: GetPet/HasLivingPet/GetPetHealthPct
           (Player::GetPet; Pet IS-A Unit). First use of the pet seam (S19 deepens
           it). Pet summon/revive relevance (ACTION_HIGH+5=25) outranks the
           rotation so BM/Survival fix a dead pet before dpsing.
[WIRE]   AiFactory: CLASS_HUNTER registers Hunter context, installs "hunter" +
           spec strategy in combat, "hunter nc" in non-combat. (Survival is the
           one melee hunter spec; its strategy uses cast melee:: for Raptor/Carve.)
[VERIFY] 6 new files + ServerFacade/factory edits: whole module braces/parens
           balanced (77 files); guards unique; includes resolve (Pet.h added);
           every override defined; spec names match CombatStrategyForSpec; bridge
           decl==def; POWER_FOCUS=2 + hunter spec order (0/1/2) confirmed; pet-
           upkeep rotation traced (no pet -> Revive Pet@25 beats rotation). NOT
           compile-tested (no toolchain/8.3.7 DB2); spell NAMES from wowhead BfA.
[DOC]    Checklist: Hunter section marked; progress 99 -> 113. (No cmangos
           hunter/ folder existed in the ref clone; authored from BfA research +
           the established per-class pattern.)

================================================================================
 2026-06-08  PHASE C - S11  ROGUE (Assassination / Outlaw / Subtlety + stealth/CP)
================================================================================
[S11]    BfA 8.3 Rogue, all 3 specs, on the real engine (research: wowhead.com /
           Ravenholdt BfA quickstart):
           classes/rogue/RogueTriggers.{h,cpp} - combo points full(>=5)/low(<=1)
             (RAW POWER_COMBO_POINTS=4 count), energy high (power::3), stealthed/
             not stealthed (Stealth or Shadow Dance aura), target low health(<=35),
             rupture/garrote absent, slice and dice absent. + RegisterRogueTriggers.
           classes/rogue/RogueStrategies.{h,cpp}:
             Generic (Crimson Vial self-heal, Kick interrupt, Cheap Shot stealth
               opener),
             Assassination (Mutilate builder -> Envenom finisher @5CP; Garrote +
               Rupture bleeds; Vendetta CD; Fan of Knives AoE),
             Outlaw (Sinister Strike builder -> Dispatch finisher; Roll the Bones/
               Slice and Dice; Adrenaline Rush CD; Blade Flurry AoE),
             Subtlety (Backstab/Shadowstrike builders -> Eviscerate finisher;
               Nightblade bleed; Symbols of Death + Shadow Dance CDs; Shuriken
               Storm AoE),
             NonCombat (Stealth upkeep out of combat).
           classes/rogue/RogueAiObjectContext.{h,cpp} - bundle + spec map
             (0=assassination,1=outlaw,2=subtlety).
[DESIGN] FINISHERS gated by "combo points full" (>=5 CP) at relevance 15 so they
           outrank builders (11) and only fire when CP is capped - models the
           builder/finisher loop. Combo points read as a RAW count via
           ServerFacade::GetPower (max 5), not a percent.
[WIRE]   AiFactory: CLASS_ROGUE registers Rogue context, installs "rogue" + spec
           strategy in combat, "rogue nc" in non-combat.
[VERIFY] 6 new files + factory edits: whole module braces/parens balanced (83
           files); guards unique; includes resolve; every override defined; spec
           names match CombatStrategyForSpec; bridge decl==def; POWER_ENERGY=3 +
           POWER_COMBO_POINTS=4 + rogue spec order (0/1/2) confirmed; finisher
           rotation traced (5 CP -> Envenom@15 beats builders). NOT compile-tested
           (no toolchain/8.3.7 DB2); spell NAMES from wowhead BfA. (cmangos ref
           clone has NO rogue/ folder -> authored from research + pattern.)
[DOC]    Checklist: Rogue section marked; progress 113 -> 127.

================================================================================
 2026-06-08  PHASE C - S12  PRIEST (Discipline/Holy heal + Shadow dps)
================================================================================
[S12]    BfA 8.3 Priest, all 3 specs (TWO healers + one dps), on the real engine
           (research: wowhead.com / icy-veins spell lists):
           classes/priest/PriestTriggers.{h,cpp} - insanity high (power::13),
             target low health (SW:Death <=20), shadow word pain / vampiric touch
             absent, shadowform absent, atonement low (heal target lacks Atonement
             - checks the party-member-to-heal unit). + RegisterPriestTriggers.
           classes/priest/PriestStrategies.{h,cpp}:
             Generic (Power Word: Shield a hurt ally, Desperate Prayer self-save,
               Shadow Word: Death execute),
             Discipline (atonement: PWS to apply Atonement + Penance/Pain
               Suppression direct heals + Smite/SW:Pain damage->atonement heal),
             Holy (direct: Holy Word: Serenity/Flash Heal/Heal tiered + Guardian
               Spirit emergency; Smite filler),
             Shadow (DoTs SW:Pain + Vampiric Touch, Mind Blast/Mind Flay fillers,
               Void Eruption/Devouring Plague Insanity spend, Shadowfiend CD),
             NonCombat (Shadowform + Power Word: Fortitude upkeep).
           classes/priest/PriestAiObjectContext.{h,cpp} - bundle + spec map
             (0=discipline,1=holy,2=shadow).
[WIRE]   AiFactory: CLASS_PRIEST registers Priest context, installs "priest" +
           spec strategy in combat, "priest nc" in non-combat. Heals use
           ACTION_*_HEAL tiers; atonement upkeep at ACTION_LIGHT_HEAL+5.
[FIX]    Caught + fixed a missing "target low health" trigger in the Priest
           context (referenced by the SW:Death execute). Added a ROBUST whole-
           module check: every TriggerNode name referenced by every class
           strategy now provably resolves to a registered trigger (core or class).
           All 6 classes pass.
[VERIFY] 6 new files + factory edits: whole module braces/parens balanced (89
           files, ~8526 LOC); guards unique; includes resolve; every override
           defined; spec names map to registered strategies; bridge decl==def;
           POWER_INSANITY=13 + priest spec order (0/1/2) confirmed. NOT compile-
           tested. cmangos ref has no priest/ folder -> authored from research.
[DOC]    Checklist: Priest section marked; progress 127 -> 141.

================================================================================
 2026-06-08  PHASE C - S13  SHAMAN (Elemental/Enhancement dps, Restoration heal)
================================================================================
[S13]    BfA 8.3 Shaman, all 3 specs, on the real engine (research: wowhead.com /
           maxroll spell lists):
           classes/shaman/ShamanTriggers.{h,cpp} - maelstrom high (power::11),
             flame shock absent, maelstrom weapon high (Enh aura), windfury/
             flametongue weapon absent (imbues), lightning shield absent.
             + RegisterShamanTriggers.
           classes/shaman/ShamanStrategies.{h,cpp}:
             Generic (Wind Shear interrupt, Healing Surge self-heal),
             Elemental (ranged: Flame Shock DoT, Lava Burst on CD, Earth Shock at
               high Maelstrom, Lightning Bolt filler, Fire Elemental CD, Chain
               Lightning AoE),
             Enhancement (melee: Stormstrike/Lava Lash, instant Lightning Bolt at
               Maelstrom Weapon stacks, Flame Shock, Feral Spirit CD, Crash
               Lightning AoE),
             Restoration (healer: Riptide HoT + Healing Surge/Wave + Chain Heal;
               Lightning Bolt damage filler when nobody hurt),
             NonCombat (Windfury + Flametongue weapon imbues, Lightning Shield).
           classes/shaman/ShamanAiObjectContext.{h,cpp} - bundle + spec map
             (0=elemental,1=enhancement,2=restoration).
[WIRE]   AiFactory: CLASS_SHAMAN registers Shaman context, installs "shaman" +
           spec strategy in combat, "shaman nc" in non-combat (weapon imbues).
[VERIFY] 6 new files + factory edits: whole module braces/parens balanced (95
           files, ~9075 LOC); guards unique; includes resolve; every override
           defined; spec names map; bridge decl==def; robust trigger-resolution
           check passes for ALL 7 classes; POWER_MAELSTROM=11 + shaman spec order
           (0/1/2) confirmed; Ele rotation traced (Earth Shock spends high
           Maelstrom first). NOT compile-tested. cmangos ref has no shaman/ folder.
[DOC]    Checklist: Shaman section marked; progress 141 -> 155.

================================================================================
 2026-06-08  PHASE C - S14  MAGE (Arcane / Fire / Frost dps + conjure/polymorph)
================================================================================
[S14]    BfA 8.3 Mage, all 3 specs, on the real engine (research: wowhead.com BfA
           mage-changes + icy-veins):
           classes/mage/MageTriggers.{h,cpp} - arcane charges high (power::16),
             clearcasting / hot streak / brain freeze / fingers of frost procs
             (named auras), target low health (Scorch execute), arcane intellect
             absent. + RegisterMageTriggers.
           classes/mage/MageStrategies.{h,cpp}:
             Generic (Counterspell interrupt, Ice Block self-save, Frost Nova
               root when an enemy reaches melee),
             Arcane (Arcane Blast builder, Arcane Missiles on Clearcasting,
               Arcane Barrage charge dump, Arcane Power CD, Arcane Explosion AoE),
             Fire (Fireball filler, Pyroblast on Hot Streak, Fire Blast banker,
               Scorch execute, Combustion CD, Flamestrike AoE),
             Frost (Frostbolt filler, Flurry on Brain Freeze, Ice Lance on
               Fingers of Frost, Frozen Orb/Icy Veins CDs, Blizzard AoE),
             NonCombat (Arcane Intellect buff + Conjure Refreshment).
           classes/mage/MageAiObjectContext.{h,cpp} - bundle + spec map
             (0=arcane,1=fire,2=frost).
[WIRE]   AiFactory: CLASS_MAGE registers Mage context, installs "mage" + spec
           strategy in combat, "mage nc" in non-combat (Intellect/conjure).
[VERIFY] 6 new files + factory edits: whole module braces/parens balanced (101
           files, ~9600 LOC); guards unique; includes resolve; every override
           defined; spec names map; bridge decl==def; robust trigger-resolution
           passes for ALL 8 classes; POWER_ARCANE_CHARGES=16 + mage spec order
           (0/1/2) confirmed; Fire rotation traced (Hot Streak -> instant
           Pyroblast wins). NOT compile-tested. cmangos ref has no mage/ folder.
[DOC]    Checklist: Mage section marked; progress 155 -> 169.

================================================================================
 2026-06-08  PHASE C - S15  WARLOCK (Affliction/Demonology/Destruction + pet/shards)
================================================================================
[S15]    BfA 8.3 Warlock, all 3 specs, on the real engine (research: wowhead.com /
           maxroll / community megathreads):
           classes/warlock/WarlockTriggers.{h,cpp} - soul shards high (power::7),
             agony/corruption/immolate absent (DoTs), demonic core proc, no
             warlock pet (ServerFacade pet seam). + RegisterWarlockTriggers.
           classes/warlock/WarlockStrategies.{h,cpp}:
             Generic (Drain Life self-heal, Unending Resolve, Fear melee enemy,
               summon Imp if no pet),
             Affliction (Agony/Corruption DoTs, Unstable Affliction shard spend,
               Summon Darkglare CD, Drain Soul filler, Seed of Corruption AoE),
             Demonology (Demonbolt on Demonic Core, Call Dreadstalkers, Hand of
               Gul'dan shard spend, Summon Demonic Tyrant CD, Implosion AoE,
               Shadow Bolt builder),
             Destruction (Immolate DoT, Conflagrate, Chaos Bolt shard spend,
               Summon Infernal CD, Incinerate filler, Rain of Fire AoE),
             NonCombat (Summon Imp + Create Healthstone).
           classes/warlock/WarlockAiObjectContext.{h,cpp} - bundle + spec map
             (0=affliction,1=demonology,2=destruction).
[WIRE]   AiFactory: CLASS_WARLOCK registers Warlock context, installs "warlock" +
           spec strategy in combat, "warlock nc" in non-combat (pet/healthstone).
           Reuses the ServerFacade pet seam (added in S10) for "no warlock pet".
[VERIFY] 6 new files + factory edits: whole module braces/parens balanced (107
           files, ~10125 LOC); guards unique; includes resolve; every override
           defined; spec names map; bridge decl==def; robust trigger-resolution
           passes for ALL 9 classes; POWER_SOUL_SHARDS=7 + warlock spec order
           (0/1/2) confirmed; Affliction rotation traced (no pet -> Summon Imp@25
           first, then DoTs). NOT compile-tested. cmangos ref has no warlock/ folder.
[DOC]    Checklist: Warlock section marked; progress 169 -> 183.

================================================================================
 2026-06-08  PHASE C - S16  DRUID (Balance/Feral dps, Guardian tank, Resto heal)
================================================================================
[S16]    BfA 8.3 Druid - the FOUR-spec class with FORMS - on the real engine
           (research: wowhead.com / method / maxroll):
           classes/druid/DruidTriggers.{h,cpp} - astral power high (power::8),
             rage high (power::1), combo points full (raw POWER_COMBO_POINTS),
             moonkin/cat/bear form absent, ironfur absent, moonfire/sunfire/rake/
             rip/thrash absent. + RegisterDruidTriggers.
           classes/druid/DruidStrategies.{h,cpp}:
             Generic (Barkskin defensive),
             Balance (Moonkin Form -> Moonfire/Sunfire DoTs, Wrath filler,
               Starsurge AP spend, Starfall AoE),
             Feral (Cat Form -> Rake/Rip bleeds, Shred builder, Ferocious Bite
               finisher @5 CP, Tiger's Fury CD, Swipe AoE),
             Guardian (Bear Form -> Mangle, Thrash bleed, Ironfur + Frenzied
               Regeneration mitigation, Maul rage dump),
             Restoration (caster -> Rejuvenation/Swiftmend/Regrowth/Wild Growth
               on party member to heal; Wrath filler),
             NonCombat (Mark of the Wild buff).
           classes/druid/DruidAiObjectContext.{h,cpp} - bundle + 4-spec map
             (0=balance,1=feral,2=guardian,3=restoration).
[DESIGN] FORM upkeep is the top combat priority (ACTION_HIGH+5=25): each spec
           shifts into Moonkin/Cat/Bear before its rotation; Resto stays caster.
           SpecRoles already had Druid as the only 4-spec entry (index 3 = Resto).
[WIRE]   AiFactory: CLASS_DRUID registers Druid context, installs "druid" + spec
           strategy in combat, "druid nc" in non-combat.
[VERIFY] 6 new files + factory edits: whole module braces/parens balanced (113
           files, ~10691 LOC); guards unique; includes resolve; every override
           defined (10 incl. 4 specs); spec names map; bridge decl==def; robust
           trigger-resolution passes for ALL 10 classes; POWER_LUNAR_POWER=8 +
           druid spec order (0/1/2/3) confirmed; Feral rotation traced (Cat Form
           shift -> bleeds -> finisher). NOT compile-tested. cmangos ref had a
           druid/ folder but this is authored fresh for BfA 8.3 + our engine.
[DOC]    Checklist: Druid section marked; progress 183 -> 203.
           >>> ALL 10 reference-backed classes done. S17 Monk + S18 Demon Hunter
               (NEW, no cmangos ref) remain for the 12-class milestone. <<<

================================================================================
 2026-06-08  PHASE C - S17  MONK (NEW - Brewmaster/Mistweaver/Windwalker)
================================================================================
[S17]    BfA 8.3 Monk - NEW class (no cmangos reference; authored from wowhead/
           community research) - all 3 specs on the real engine:
           classes/monk/MonkTriggers.{h,cpp} - chi high(>=4)/enough(>=2) (raw
             POWER_CHI=12), energy high (power::3), target low health (Touch of
             Death <=15%), ironskin brew absent, shuffle absent.
             + RegisterMonkTriggers.
           classes/monk/MonkStrategies.{h,cpp}:
             Generic (Expel Harm self-heal, Spear Hand Strike interrupt, Touch of
               Death execute),
             Brewmaster (tank: Keg Smash/Blackout Strike/Tiger Palm, Ironskin +
               Purifying Brew mitigation, Shuffle upkeep, Breath of Fire, SCK AoE),
             Mistweaver (healer: Renewing Mist/Vivify/Enveloping Mist/Essence Font
               on party member to heal; Tiger Palm fistweave filler),
             Windwalker (dps: Tiger Palm Chi gen, Rising Sun Kick + Blackout Kick
               Chi spend, Fists of Fury CD, Spinning Crane Kick AoE),
             NonCombat (Legacy of the Emperor stat buff).
           classes/monk/MonkAiObjectContext.{h,cpp} - bundle + spec map
             (0=brewmaster,1=mistweaver,2=windwalker).
[WIRE]   AiFactory: CLASS_MONK registers Monk context, installs "monk" + spec
           strategy in combat, "monk nc" in non-combat.
[VERIFY] 6 new files + factory edits: whole module braces/parens balanced (119
           files, ~11206 LOC); guards unique; includes resolve; every override
           defined; spec names map; bridge decl==def; robust trigger-resolution
           passes for ALL 11 classes; POWER_CHI=12/POWER_ENERGY=3 + monk spec
           order (0/1/2) confirmed; Windwalker rotation traced (Chi spend wins).
           NOT compile-tested. AUTHORED FROM RESEARCH (no reference), like DH next.
[DOC]    Checklist: Monk section marked; progress 203 -> 223. 11/12 classes done.

================================================================================
 2026-06-08  PHASE C - S18  DEMON HUNTER (NEW - Havoc/Vengeance)
            >>>>>>>>>>  MILESTONE: ALL 12 CLASSES PLAY THEIR SPECS  <<<<<<<<<<
================================================================================
[S18]    BfA 8.3 Demon Hunter - NEW class (no cmangos ref; authored from wowhead/
           method research) - the FINAL class, only TWO specs:
           classes/demonhunter/DemonHunterTriggers.{h,cpp} - fury high (power::17),
             pain high (power::18), soul fragments high (aura), immolation aura
             absent, demon spikes absent. + RegisterDemonHunterTriggers.
           classes/demonhunter/DemonHunterStrategies.{h,cpp}:
             Generic (Consume Magic interrupt, Fel Rush gap-closer, Immolation
               Aura upkeep),
             Havoc (Demon's Bite gen -> Chaos Strike spend, Eye Beam CD, Blade
               Dance AoE, Metamorphosis CD),
             Vengeance (Fracture gen -> Soul Cleave spend + Spirit Bomb on soul
               fragments, Demon Spikes + Fiery Brand mitigation, Sigil of Flame),
             NonCombat (no standing buff - placeholder hook).
           classes/demonhunter/DemonHunterAiObjectContext.{h,cpp} - bundle +
             2-spec map (0=havoc,1=vengeance).
[WIRE]   AiFactory: CLASS_DEMON_HUNTER registers DH context + spec strategy +
           "demon hunter nc". >>> ALL 12 CLASSES now register a context. <<<
[VERIFY] 6 new files + factory edits: whole module braces/parens balanced (125
           files, ~11650 LOC); guards unique; includes resolve; every override
           defined; spec names map; bridge decl==def; AiFactory has 12/12 class
           RegisterContext cases; robust trigger-resolution passes for ALL 12
           classes; POWER_FURY=17/POWER_PAIN=18 + DH spec order (0/1) confirmed;
           Havoc/Vengeance rotations traced. NOT compile-tested. AUTHORED FROM
           RESEARCH (no reference).
[DOC]    Checklist: Demon Hunter section marked; progress 223 -> 239. 12/12
           classes DONE. >>> PHASE C COMPLETE (S7-S18). <<< Next: Phase D depth
           (S19 pets / S20 group-raid / S21 healing / ...).

================================================================================
 2026-06-08  PHASE D - S19  PET SYSTEM DEPTH
================================================================================
[S19]    Real pet control for every pet-using class (Hunter BM/Survival, all
           Warlock, DK Unholy ghoul, Frost Mage water elemental):
           engine/ServerFacade pet-control seam: PetAttack (aggressive react +
             Attack target), PetFollow (defensive react + AttackStop),
             PetAttackingTarget, PetSpellReady, PetCastSpell. (Pet : Guardian :
             ... : Unit; uses SetReactState + Attack + GetSpellHistory.)
           pets/PetActions.{h,cpp} - "pet attack" (sic on current target) +
             "pet follow"; RegisterPetActions.
           pets/PetStrategy.{h,cpp} - "pet idle" trigger (living pet + valid
             target + pet NOT already on it) -> "pet attack" @ACTION_HIGH; the
             "pet" strategy + RegisterPetSystem (actions+trigger+strategy).
           PsychobotAIFwd.h + PsychobotAI.cpp bridge: PetAttack/PetFollow/
             PetCastSpell route through ServerFacade.
[WIRE]   AiFactory: UsesPet(class,spec) gate -> RegisterPetSystem in CreateContext
           and AddStrategy("pet") in InitCombatEngine (layered on top of the spec
           rotation; AddStrategy is a no-op for non-pet classes). Pet abilities
           still use the keystone "cast pet::<spell>"; summoning stays the class
           non-combat job (Hunter Call Pet, Warlock Summon Imp, etc.).
[VERIFY] 4 new files + seam/bridge/factory edits: whole module braces/parens
           balanced (129 files, ~12042 LOC); guards unique; module includes
           resolve (Pet.h is a core header); bridge decl==def; all trigger refs
           resolve (12 classes + pet); ReactStates/CommandStates/Pet inheritance
           confirmed in core; Unholy-DK ghoul trace OK. NOT compile-tested.
[DOC]    Checklist: pet section marked; progress 239 -> 251.

================================================================================
 2026-06-08  PHASE D - S20  GROUP / RAID COORDINATION
================================================================================
[S20]    Real group/raid combat coordination, wired through PsychobotGroupMgr:
           GetGroupAssistTarget UPGRADED to a priority order: main-assist's
             victim (MEMBER_FLAG_MAINASSIST) > main-tank's victim (MAINTANK) >
             any tank-role member's victim > leader's victim. (Reads core
             Group::GetMemberFlags.)
           GetGroupHealTarget: main-tank gets a -10% effective-health bias so a
             hurt tank is healed before equally-hurt dps.
           NEW MarkSkullTarget(bot) - the leader / main-tank / tank-role bot marks
             its current target with the Skull raid icon (RTI 7) via
             Group::SetTargetIcon so the group focuses it.
           NEW AnswerReadyCheck(bot) - bots auto-accept an in-progress ready check
             (Group::IsReadyCheckStarted + SetMemberReadyCheck).
           NEW GetFollowFormationAngle(bot) - fans bots out in an arc behind the
             master by their group-slot ordinal (no more stacking).
[WIRE]   PsychobotAI::UpdateAI now (cheap, every tick): AnswerReadyCheck + (in
           combat) MarkSkullTarget. FollowMaster uses GetFollowFormationAngle for
           the MoveFollow angle. Assist/heal target selection already routed
           through GroupMgr -> the new priorities apply to every class.
[NOTE]   Core Group API has no PUBLIC getter for the marked-target GUID, so
           "assist the skull" is realised by everyone assisting the main-assist/
           main-tank (who is the marker + on the skull). Documented limitation;
           a tiny core getter could make it literal later (Phase A-style edit).
[VERIFY] GroupMgr.{h,cpp} + PsychobotAI.cpp edits: whole module braces/parens
           balanced (129 files, ~12161 LOC); guards unique; bridge decl==def; all
           trigger refs resolve; core APIs cross-checked (SetTargetIcon(uint8,
           guid,guid,uint8); SetMemberReadyCheck(guid,bool); MEMBER_FLAG_* global
           enum; MoveFollow angle). NOT compile-tested.
[DOC]    Checklist: group/raid coordination marked; progress 251 -> 263.

================================================================================
 2026-06-08  PHASE D - S21  HEALING DEPTH
================================================================================
[S21]    Dispels/cures, smart dispel-target selection, mana awareness, tank
           priority (the latter shipped in S20) - all class-agnostic:
           engine/ServerFacade: HasDispellableAura(unit, dispelMask) - scans
             GetAppliedAuras(), checks each non-positive aura's SpellInfo->Dispel
             against (1<<DispelType) mask; GetManaPct(unit) (POWER_MANA pct, 0 if
             no mana bar).
           PsychobotGroupMgr::GetGroupDispelTarget(bot, dispelMask) - self-first
             then group scan for a member with a removable debuff.
           values/CoreValues: "party member to dispel" (any DispelType) value.
           triggers/CoreTriggers: "party member needs dispel" + "low mana"
             (<25%; only trips for real mana users).
           actions/GenericSpellActions: "cast dispel" (CastDispelSpellAction -
             cures the dispel-target value; 40yd, no chase).
[WIRE]   All 5 healer specs now cure debuffs at ACTION_DISPEL relevance:
             Holy Paladin -> Cleanse; Disc + Holy Priest -> Purify; Resto Shaman
             -> Cleanse Spirit; Resto Druid -> Nature's Cure; Mistweaver Monk ->
             Detox. (Cure relevance 50 sits above routine heals, below emergency
             heals.) Heal-target tank priority + main-tank heal bias already
             added in S20.
[VERIFY] seam/groupmgr/values/triggers/actions + 5 strategy edits: whole module
           braces/parens balanced (129 files, ~12362 LOC); guards unique; bridge
           decl==def; ALL trigger refs resolve (incl. new dispel trigger);
           ACTION_DISPEL=50 confirmed; core dispel API cross-checked (GetApplied
           Auras / AuraApplication::GetBase/IsPositive / SpellInfo::Dispel;
           DispelType MAGIC=1/CURSE=2/DISEASE=3/POISON=4); SpellAuras.h is a core
           header. NOT compile-tested. Resto-Druid cure trace OK.
[DOC]    Checklist: healing depth marked; progress 263 -> 275.

================================================================================
 2026-06-08  PHASE D - S22  NON-COMBAT WORLD
================================================================================
[S22]    Out-of-combat housekeeping (self-contained, safe):
           world/PsychobotWorldMgr.{h,cpp} - CanRest (safe-state gate); Rest
             (eat/drink: SetFullHealth + refill mana for mana users when hurt and
             out of combat); Repair (self-repair via Player::DurabilityRepairAll,
             pays from the bot's coinage). FRAMEWORK HOOKS (return false until the
             first on-server pass, need live world objects/packets): MountForTravel,
             SellJunk, LootNearby, HandleQuests, GatherNearby.
           actions/WorldActions.{h,cpp} - "rest" + "repair" engine actions.
           strategies/GenericStrategies: NEW RestStrategy ("rest") non-combat
             default actions (rest @ACTION_NORMAL, repair @ACTION_IDLE+1).
[WIRE]   AiFactory: RegisterWorldActions in CreateContext; every bot's non-combat
           engine now runs "follow" + "rest" (rest outranks follow when hurt;
           repair is cheap upkeep). Buff upkeep already per-class (S7-S18 NC
           strategies). Buffs/mount/vendor/loot/quest/gather framework in place.
[VERIFY] 4 new files + strategy/factory edits: whole module braces/parens
           balanced (133 files, ~12647 LOC); guards unique; bridge decl==def; all
           trigger/strategy refs resolve; core APIs cross-checked
           (DurabilityRepairAll(bool,float,bool); SetFullHealth; SetPower(Powers,
           int32); GetMaxPower/GetPowerPct). NOT compile-tested. Hurt-bot rest
           trace OK.
[NOTE]   Rest restores to full deterministically (models food/water) rather than
           consuming real bag items; a real-item version + mount-from-collection +
           vendor/loot/quest/gather are the documented first-on-server tasks.
[DOC]    Checklist: non-combat world marked; progress 275 -> 287.

================================================================================
 2026-06-08  PHASE D - S23  TRAVEL (minimal: local nav + robust follow)
================================================================================
[S23]    travel/PsychobotTravelMgr.{h,cpp}:
           WorldPosition - lightweight {mapId,x,y,z,o} struct (IsValid()).
           TravelNode    - named WorldPosition (seed of the 8.3 travel graph).
           TravelMgr: GetPosition; DistanceTo (same-map, huge if maps differ);
             MoveTo (cross-map or >teleportRange -> TeleportTo; within walkRange
             -> pathed MovePoint; otherwise path toward); FollowMasterTravel
             (cross-map or >80yd leash -> TeleportTo the master, else return
             false so normal MoveFollow runs); AddNode/GetNode seed registry.
[WIRE]   PsychobotAI::FollowMaster now calls TravelMgr::FollowMasterTravel first
           (handles cross-map + far catch-up via teleport), then falls back to
           the S20 formation MoveFollow when close. This fixes the old "bail on
           cross-map" follow gap.
[VERIFY] 2 new files + FollowMaster edit: whole module braces/parens balanced
           (135 files, ~12868 LOC); guards unique; bridge decl==def; all trigger
           refs resolve; core APIs cross-checked (TeleportTo(uint32,f,f,f,f,uint32);
           MovePoint(uint32,f,f,f,bool); GetMapId; GetDistance). NOT compile-tested.
           Cross-map follow trace OK.
[NOTE]   Minimal by design: real node-graph navigation across 8.3 zones (long-
           distance pathing, flight paths, transports) is the documented later
           expansion; AddNode/GetNode + WorldPosition are the seed for it.
[DOC]    Checklist: travel marked; progress 287 -> 299.

================================================================================
 2026-06-08  PHASE D - S24  PvP / BATTLEGROUNDS
================================================================================
[S24]    pvp/PsychobotPvpMgr.{h,cpp}:
           BgFlavor (FlagCarry/BaseCapture/Resource/Payload/Arena) + GetBgFlavor
             (classifies the bot's BG by BattlegroundTypeId: WS/TP/EY/TK flag;
             AB/BFG/DG base; AV/IC resource; SM payload; arenas).
           InActiveBattleground (joined + STATUS_IN_PROGRESS), InArena.
           GetPvpTarget - nearest valid ENEMY PLAYER within range
             (SelectNearestPlayer + IsValidAttackTarget).
           Per-BG objective tactics (DoFlag/Base/Resource/Arena) = framework
             hooks (need live BG objective objects/positions; on-server pass).
[WIRE]   PsychobotAI::GetCurrentTarget now, when in an active BG, returns the
           nearest enemy player -> EVERY class strategy then reaches/casts on it
           through the existing "current target" value (no per-class edits). So
           all 12 classes fight in BGs/arenas immediately; objective play is the
           documented next layer.
[VERIFY] 2 new files + GetCurrentTarget edit: whole module braces/parens balanced
           (137 files, ~13031 LOC); guards unique; bridge decl==def; core APIs
           cross-checked (SelectNearestPlayer(float); GetBattleground; GetStatus;
           all BG type ids: WS/TP/EY/TK/AB/BFG/DG/AV/IC/SM). NOT compile-tested.
           Active-WSG target trace OK.
[NOTE]   OutdoorPvP + per-BG objective behaviour (carry flag, cap base, take boss/
           vehicle) are framework hooks - they need live objective objects that
           can't be statically resolved; the always-safe "fight the nearest enemy"
           PvP behaviour ships now.
[DOC]    Checklist: PvP/BG marked; progress 299 -> 311.

================================================================================
 2026-06-08  PHASE D - S25  DUNGEON / RAID scripted strategies (boss awareness)
================================================================================
[S25]    dungeon/PsychobotDungeonMgr.{h,cpp}:
           InDungeonOrRaid (Map::IsDungeon/IsRaid); FightingBoss (current target
             is a DungeonBoss/worldboss); BossCastingInterruptible (boss's active
             generic/channeled cast has SPELL_INTERRUPT_FLAG_INTERRUPT).
           EncounterScript framework: RegisterEncounter(creatureEntry, fn) +
             RunEncounterScript(bot) (runs the boss's script each combat tick) +
             InitEncounters() (seeded; example registration pattern shown).
[WIRE]   PsychobotAI::UpdateAI runs RunEncounterScript in combat while in an
           instance (no-op until per-boss scripts are filled in). mod_psychobot
           OnConfigLoad calls InitEncounters(). Boss interrupts already work via
           the per-class "target casting" trigger (the boss IS the current target),
           so bots kick interruptible boss casts with no extra wiring.
[VERIFY] 2 new files + AI/module edits: whole module braces/parens balanced (139
           files, ~13222 LOC); guards unique; bridge decl==def; core APIs cross-
           checked (IsDungeon/IsRaid; IsDungeonBoss/isWorldBoss; GetEntry;
           GetCurrentSpell(uint32); ToCreature; SPELL_INTERRUPT_FLAG_INTERRUPT=
           0x08). NOT compile-tested. Boss-interrupt + encounter-tick trace OK.
[NOTE]   Generic boss awareness ships (detect boss, interrupt its casts, run the
           encounter tick). Concrete per-encounter reactions (move out of THIS
           ground effect, soak/spread/stack, add-switch) need encounter-specific
           positions/spell ids -> EncounterScript registry hooks for the first
           on-server pass with a couple of key encounters added then.
[DOC]    Checklist: dungeon/raid marked; progress 311 -> 323.

================================================================================
 2026-06-08  PHASE D - S26  AHBOT (auction posting/buying for 8.3 item data)
================================================================================
[S26]    PsychobotAhBot deepened from scaffold to a real, structured ahbot
           (clean-room re-impl of the cmangos ahbot/ 14-file seller LOGIC):
           IsSellable(entry) - candidate gate: skip quest/key items, soulbound &
             quest-bound (BIND_ON_ACQUIRE/BIND_QUEST), zero-merchant-value trash,
             quality > MaxQuality, below MinItemLevel.
           PriceItem(entry) - quality-aware buyout model: base SellPrice *
             PriceMultiplier * (1 + 0.25*quality).
           BuildListings() - pure/deterministic scan of sObjectMgr item-template
             store -> AhListing{entry,stackCount,auctions,unitPrice}, throttled by
             MaxListingsPerCycle. (Unit-testable; no AH access.)
           Update()/RunCycle() - cycle on IntervalSeconds; PostAuction() is the
             single live step (create Item* + AuctionHouseObject::AddAuction under
             an AH-owner account) - gated + documented for the on-server pass.
[CONF]   Added Psychobot.AhBot.{MaxListingsPerCycle,MinItemLevel,MaxQuality,
           PriceMultiplier} to mod_psychobot.conf.dist (build-glue: no MyCode hdr).
[VERIFY] AhBot.{h,cpp} rewrite + conf: whole module braces/parens balanced (139
           files); methods declared==defined; core APIs cross-checked
           (sObjectMgr; GetItemTemplateStore/GetItemTemplate; ItemTemplateContainer
           = unordered_map<uint32,ItemTemplate>; GetSellPrice/GetQuality/
           GetMaxStackSize/GetBaseRequiredLevel/GetBonding/GetClass; ITEM_CLASS_*,
           BIND_*; GetFloatDefault). NOT compile-tested.
[NOTE]   Full ahbot decision logic (what to sell + how to price) is REAL and
           verifiable; only the AddAuction packet/DB step is a gated hook (needs
           owner account + client item DB2). Buying under-priced auctions is a
           later hook (needs live AH listings).
[DOC]    Checklist: ahbot (14 ref files) marked; progress 323 -> 337.

================================================================================
 2026-06-08  PHASE D - S27  PERSISTENCE + COMMANDS
================================================================================
[S27]    world/PsychobotDbStore.{h,cpp} - per-bot saved combat-strategy overrides
           keyed by character GUID (Get/Has/Set/Toggle/Clear). In-memory store is
           real; Save/Load are documented DB hooks (characters-DB table + prepared
           statements; on-server pass). Re-applied on spec rebuild.
         PsychobotAI command surface: ToggleCombatStrategy (persists via DbStore +
           Engine Add/Remove), OrderAttackMasterTarget, OrderStay (+ _staying flag
           that suppresses follow), OrderFollow, OrderCast (master's target/self),
           ListCombatStrategies.
         PsychobotMgr grammar: OrderFollow/Stay/Attack/Cast/ToggleStrategy apply
           to every bot the master owns (ForEachOwnedBot helper) with status text.
         mod_psychobot commands: .psychobot follow|stay|attack|cast <spell>|
           strategy <name>|help (help text lists the full grammar).
[VERIFY] 2 new files + AI/Mgr/loader edits: whole module braces/parens balanced
           (141 files, ~13724 LOC); guards unique; bridge decl==def; DbStore/AI/Mgr
           methods declared==defined; 6 new chat handlers wired; ListCombatStrategies
           const-correct vs Engine::ListStrategies() const. NOT compile-tested.
           strategy-toggle trace OK.
[NOTE]   Saved strategies persist for the SESSION (in-memory) now; cross-relog
           persistence is the Save/Load DB hook (needs a DB table; on-server).
           ChatHelper item/spell-LINK parsing (parse a [Spell]/[Item] hyperlink
           in chat) is a later refinement; "cast <name>" by spell name works now.
[DOC]    Checklist: persistence + commands marked; progress 337 -> 351.

================================================================================
 2026-06-08  PHASE D - S28  PHASE A CORE EDITS (socketless login) - DEFERRED
================================================================================
[S28]    RESEARCHED but DEFERRED by user decision (riskiest step: edits CORE
           files, cannot be compile-tested here). Findings recorded for the
           on-desktop pass:
         - WorldSession ctor takes std::shared_ptr<WorldSocket> sock -> a bot
           passes nullptr (socketless). World::AddSession registers it;
           WorldSession::HandlePlayerLogin(LoginQueryHolder*) loads the char.
         - The login/update path is ALREADY almost null-safe: SendPacket() guards
           `if (!m_Socket[conIdx]) return;` (logs an error then no-ops), and every
           other m_Socket[...] deref in WorldSession.cpp is null-guarded.
         - THE ONE REAL CRASH: WorldSession::Update (WorldSession.cpp ~line 332)
             if (IsConnectionIdle()) m_Socket[CONNECTION_TYPE_REALM]->CloseSocket();
           IsConnectionIdle() = (m_timeOutTime <= 0 && !m_inQueue) -> a socketless
           bot eventually trips this and dereferences a null socket -> crash.
         READY-TO-APPLY core patch (1 line, NO MyCode header - edited core file):
             if (IsConnectionIdle() && m_Socket[CONNECTION_TYPE_REALM])
                 m_Socket[CONNECTION_TYPE_REALM]->CloseSocket();
         (Optional log-spam fix: skip the SendPacket "non existent socket" error
           log when the session is flagged as a bot.)
         Module side (to build on-desktop): PsychobotLoginMgr - construct a bot
           WorldSession(nullptr socket) for an offline bot-account char, AddSession,
           run HandlePlayerLogin via a LoginQueryHolder; then PsychobotPopulationMgr
           tops up toward minRandomBots. NOT built here (no compile/runtime).
[DECISION] User chose "defer" - leave core untouched; do S29 hardening next; S28
           becomes the final on-desktop task with compile-testing available.

================================================================================
 2026-06-08  S29  HARDENING + FINAL AUDIT  (done out of order, after S28 deferral)
================================================================================
[S29]    Full static end-to-end audit of mod-psychobot (141 files, ~13724 LOC):
          1. brace/paren balance whole module ............. ALL BALANCED
          2. include guards ............................... ALL UNIQUE
          3. bridge decl==def ............................. MATCH
          4. all 12 classes wired in AiFactory ............ 12/12
          5. every TriggerNode name resolves (classes+pet)  ALL RESOLVE
          6. every NextAction name resolves to a registered
             action (cast family/movement/pet/world) ...... ALL RESOLVE (19 actions)
          7. every CombatStrategyForSpec return is a
             registered strategy (12 classes/36 specs) ..... ALL VALID
          8. MyCode header on every from-scratch file ...... ALL HEADERED
          9. managers are instance() singletons ............ 4/4
         10. hot-path safety: AI tick guards (in-world/250ms
             throttle/ShouldBotBeActive); Engine kMaxDepth=5
             recursion guard + null-context guards;
             ServerFacade null-guards ..................... PRESENT
         11. memory: engine raw new (ActionBasket/ActionNode/
             NextAction) all owned by Queue/_ownedNodes/
             Destroy chains ............................... OK (verified S2/S3)
[STILL]  NOT compile-tested (no toolchain / no 8.3.7 client data) - first real
           compile is the desktop truth check. Remaining for on-desktop:
           S28 socketless login (1-line core patch documented) + the documented
           framework hooks (mount/vendor/loot/quest/gather; per-BG objectives;
           per-encounter reactions; ahbot AddAuction; DbStore DB persist;
           chat link parsing).
[DOC]    Final audit recorded. Project static-complete pending compile.

================================================================================
 2026-06-08  S28 DEEP RESEARCH (web) - socketless bot login  [pre-implementation]
================================================================================
[RESEARCH] User clarified: DO NOT skip S28 - deep-research it first (web + a
  second-AI prompt) before implementing. Findings:
  - EVERY working ike3-lineage playerbot (ZhengPeiRu21, liyunfan1223/mod-playerbots)
    REQUIRES A CUSTOM CORE BRANCH ("requires custom changes to AzerothCore" - they
    ship a forked azerothcore-wotlk/tree/Playerbot). => socketless login is NOT a
    clean module-only feature; core edits are expected/normal here. Confirms my
    earlier finding and means the scope is bigger than a 1-line patch.
  - Canonical login pattern (ike3/mangos PlayerbotMgr::AddPlayerBot, TC-adapted):
      WorldSession* s = new WorldSession(accountId, "", bnetAcc, /*sock*/nullptr,
                          SEC_PLAYER, expansion, mute, locale, recruiter, isARecruiter);
      LoginQueryHolder* holder = new LoginQueryHolder(accountId, guid);
      holder->Initialize();              // queue the character SELECTs
      // run holder (sync or via CharacterDatabase.DelayQueryHolder + callback)
      s->HandlePlayerLogin(holder);      // builds the Player
      sWorld->AddSession(s);             // register (order/threading core-specific)
  - TC 8.3 WorldSession ctor (verified in our tree, WorldSession.h:910):
      WorldSession(uint32 id, std::string&& name, uint32 battlenetAccountId,
        std::shared_ptr<WorldSocket> sock, AccountTypes sec, uint8 expansion,
        time_t mute_time, ...)
  - Our tree's login path is already mostly null-socket-safe EXCEPT
    WorldSession::Update ~line332: m_Socket[REALM]->CloseSocket() under
    IsConnectionIdle() (= m_timeOutTime<=0 && !m_inQueue) -> null deref crash.
  - OPEN QUESTIONS for the 2nd AI (in the prompt): exact TC-3.3.5/8.x ctor arg
    list + how to run LoginQueryHolder sync vs async on TC; whether to add an
    isBot flag to WorldSession (vs leave null-socket); how AddSession + the
    World::UpdateSessions map interacts with a socketless session each tick; how
    real forks suppress the SendPacket "non existent socket" log spam; KickPlayer/
    timeout handling for bots; and the safest set of CORE edits for TC BfA 8.3.
[NEXT] Awaiting the user's relay of a second-AI answer to this prompt, then
  implement S28 against the agreed core-edit set (edited core files = NO MyCode
  header per the license rule).

================================================================================
 S28 IMPLEMENTED - SOCKETLESS BOT LOGIN (the only core-editing step)  [2026-06-08]
================================================================================
User confirmed THIS workspace is the true source (disregard the earlier
"test build" error report - those files do not exist here). Applied the agreed
core-edit set, adapted to THIS core's REAL signatures (verified each in source,
not assumed). Notable adaptations vs the generic second-AI advice:
  * EXPANSION_BATTLE_FOR_AZEROTH = 7 here (used CURRENT_EXPANSION, not literal 8).
  * KickPlayer() takes NO args in this core (not KickPlayer(string)).
  * GetRemoteAddress() returns a stored m_Address string -> NO socket deref ->
    Claude's "Edit 6" unnecessary, skipped.
  * Reused the core's OWN async login: BotLogin assigns _charLoginCallback and
    lets the existing ProcessQueryCallbacks() fire HandlePlayerLogin (cleaner &
    lower-risk than a hand-rolled DelayQueryHolder lambda).
  * Removal: World::RemoveSession relies on Update() returning false on socket
    close; bots have no socket, so added m_botRemove + SetBotRemove() and made
    Update() return false ONCE when flagged (avoids a session leak; the no-op
    KickPlayer for bots meant RemoveSession alone would never erase them).

CORE EDITS (NO MyCode header):
  WorldSession.h, WorldSession.cpp, World.cpp, CharacterHandler.cpp  (see checklist S28).
MODULE (MyCode header on new files):
  + src/PsychobotLoginMgr.{h,cpp}; edits to PsychobotMgr.{h,cpp}, mod_psychobot.cpp.

VERIFICATION (static): all core symbols/signatures cross-checked against source
(WorldSession ctor, m_playerLoading type, DelayQueryHolder(holder), _charLoginCallback
+ ProcessQueryCallbacks order, CharacterCacheEntry fields, AccountMgr GetName/GetSecurity,
ObjectAccessor::FindConnectedPlayer, World AddSession/FindSession). WorldSession ctor
already guards `if (sock)` -> nullptr socket is safe at construction.
CANNOT COMPILE here (no toolchain/client) - first real build + crash test = desktop.
[NEXT] S29 - hardening + final audit (awaiting user authorization).

================================================================================
 S29 COMPLETE - HARDENING + FINAL AUDIT  [2026-06-09]
================================================================================
Ran a 12-axis whole-module STATIC audit (see checklist S29 for the matrix):
relevance constants, bridge decl==def (13/13), AiFactory 12-class wiring, per-class
files, and the full NAME-RESOLUTION proof: 61/61 strategies, 14/14 action bases,
80/80 trigger bases ALL resolve to registered creators (0 unresolved). Spec->role
mapping BfA-accurate for all 12 classes. Memory/ownership verified (engine + context
caches free everything; multipliers/strategies are non-owning refs; no leak/double-free).
Hot path safe (recursion guard, 250ms throttle, cached objects, safe erase-in-loop).
75/75 headers guarded; singletons consistent. S28 integration end-to-end consistent.
One non-blocking note: LoginMgr::LoginBotByName is a defined-but-uncalled public API.
RESULT: static audit PASS. The module is feature-complete across S1-S29 (static).
[REMAINING - all on the user's desktop, needs toolchain + 8.3.7 client data]:
  1. First real COMPILE (the truth check) - fix any errors as they surface.
  2. In-world test: .psychobot add <offline char> -> socketless login -> follow/fight.
  3. Tune spell NAMES vs the live client DB2 (a mismatch no-ops one ability).
  4. Fill documented framework hooks (mount/vendor/loot/quest/gather; per-BG objectives;
     per-encounter reactions; ahbot AddAuction; DbStore DB persist; chat link parsing).

================================================================================
 POST-S29 CLEANUP / VERIFICATION PASS  [2026-06-09]
================================================================================
User asked to: (1) re-verify checklist files exist+coded, (2) confirm 1:1 build,
(3) tidy Dev docs/remove redundant, (4) recreate needed SQL, (5) deep-dive all
systems, (6) report.

DONE:
 * SQL (Task 4): added modules/mod-psychobot/sql/characters/psychobot_strategies.sql
   (CREATE TABLE IF NOT EXISTS, characters DB) + sql/README.txt explaining what to
   apply and why the reference's other tables (random_bots/ahbot/names/texts/cache)
   are NOT needed in our consolidated design. Wired PsychobotDbStore::Save/Load to
   REAL queries (CharacterDatabase PExecute/PQuery, fmt::sprintf printf-style,
   EscapeString on names) - persistence now round-trips (Load on AI attach, Save
   on strategy toggle). Previously these were documented no-op stubs.
 * Dead config fixed: Psychobot.FollowDistance was in conf.dist but unread - now
   wired into PsychobotAI::FollowMaster (config-driven trail distance). Config audit:
   every key read is documented; no dead keys remain.
 * README (module) corrected: command table was stale (missing S27/S28 cmds) and the
   Layout section referenced PHANTOM files (PsychobotEngine/PsychobotStrategies - the
   same wrong names from the original bogus "test build"); replaced with the real tree,
   added SQL section + honest-limitations section.
 * Dev docs tidy (Task 3): moved 8 early research/decision artifacts (Research_*.txt,
   ModuleSupport_Research*.txt) to Dev/_archive_research/ with a README. Active Dev/
   now holds only living docs (Changelog, Chatlog, Checklist, Mod-Psychobots-Ref,
   ModuleAPI_Reference, Psycho_CoreREADME, remember_prompt) + playerbots_docs/.
 * cmake message updated (full module + SQL reminder).

VERIFICATION (Tasks 1,2,5 - static):
 * 146 module files; 0 stub/empty; ~14k LoC; 0 brace/namespace mismatches across 143
   src files; every Strategy class declared==defined; all manager funcs defined;
   75/75 headers guarded; loader chain single AddSC.
 * 1:1 coverage map built (reference playerbot/ + strategy/ + ahbot/ -> ours). All
   core subsystems present. Genuine GAPS recorded honestly: flee/FleeManager (none);
   vendor/loot/quest/gather/AddAuction/per-BG-objective/per-encounter/chat-link = hooks;
   PerformanceMonitor/MemoryMonitor/CommandServer/BroadcastHelper/LLM = deferred/N-A.

================================================================================
 DEV-DOCS REFRESH PASS  [2026-06-09]
================================================================================
User flagged the chat is getting bloated/buggy and asked to make the docs (esp.
the prompt) state exactly what to read and where we are. Done:
 * REWROTE Dev/remember_prompt.txt end-to-end (was 342 lines, internally
   contradictory - some parts still said "build S28 / next S10"). New version is
   tight + consistent: a TL;DR box, an explicit ORDERED read-list (sec 0), the
   authoritative ALL-S1..S29-DONE status (sec 1), the per-class pattern, engine
   tick, file map, verification method, MyCode header, verified core facts, the
   REPO-vs-WORKSPACE warning (do not pull - we're strictly ahead), and a one-line
   resume status. It now tells a fresh session EXACTLY which files to open.
 * Fixed Dev/Psycho_CoreREADME.txt header (was "FAT PORT S9 Paladin done; Next:
   S10 Hunter") -> now "ALL 29 steps done, static; first compile on desktop".
 * Verified the other living docs: the "Awaiting GO for S10/S11..." lines are
   only in Chatlog.txt as HISTORICAL per-step entries (correct - that's the log);
   current status lives at the tail. No stale current-status markers elsewhere.
 NOTE: no code changed in this pass - documentation only.
